# ============================================
# Configuração do VPN Server
# ============================================
# Este arquivo é lido pelo systemd ao iniciar o serviço vpnserverio.service
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para /root/automais.io/vpnserver.env
# 2. Edite os valores conforme necessário
# 3. Reinicie o serviço: sudo systemctl restart vpnserverio.service
#
# Documentação completa: CONFIGURACAO_VPN_SERVER.md
# ============================================

# Nome identificador deste servidor VPN (OBRIGATÓRIO)
# Deve corresponder ao ServerName no banco de dados da API C#
# Exemplos: automais.io, vpn-server-usa, vpn-server-brasil
VPN_SERVER_NAME=automais.io

# URL da API C# principal
# IMPORTANTE: Use HTTPS se a API estiver configurada com SSL
# Exemplos:
#   - HTTPS (recomendado): https://srv01.automais.io:5001
#   - HTTPS sem porta: https://api.automais.io
#   - HTTP local (apenas desenvolvimento): http://localhost:5000
API_C_SHARP_URL=https://srv01.automais.io:5001

# Verificar certificado SSL (true/false)
# Use false apenas se usar certificado auto-assinado (não recomendado para produção)
# Padrão: true (verifica certificado)
API_C_SHARP_VERIFY_SSL=true

# Intervalo de sincronização com a API (em segundos)
# Padrão: 60 (1 minuto)
# Valores recomendados: 30 (rápido), 60 (padrão), 120 (lento)
SYNC_INTERVAL_SECONDS=60

# Porta do serviço FastAPI
# Padrão: 8000
# Se alterar, também atualize o ExecStart no arquivo de serviço systemd
PORT=8000

# Diretório de configuração do WireGuard
# Padrão: /etc/wireguard
# O usuário root deve ter permissões de escrita neste diretório
WIREGUARD_CONFIG_DIR=/etc/wireguard

